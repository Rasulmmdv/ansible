---
- name: Configure all servers
  hosts: all
  roles:
    - update
    - common
    - fail2ban
    - system-config
    - docker
    - iptables
    - common
    # - restic  # Excluded as requested
    # - portainer-agent
    # - tailscale  # Excluded as requested
    # - traefik
    # - portainer

